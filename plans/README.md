# Implementation Plans

Generated by the improve skill on 2025-07-18. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 003 | Dead code cleanup | P1 | S | — | TODO |
| 001 | Wire httpRetry into engine HTTP paths | P1 | M | — | DONE |
| 002 | Fix resource leaks and silent degradation | P1 | S | — | DONE |
| 004 | Add schema and reporter tests | P1 | M | — | DONE |

## Dependency notes

- No hard dependencies between plans — all four can run in parallel.
- Recommended order: 003 first (simplifies codebase), then 001 + 002 + 004 in parallel.
- Plan 004 (tests) provides regression safety for future changes — prioritize if only doing one.

## Findings considered and rejected

- SECURITY-02 (--notes Markdown injection): local CLI, user controls both input and output — blast radius too low
- CORRECTNESS-06 (execSync in default thermal sampler): overridden in main benchmark flow — low priority
- DX-05 (missing CLAUDE.md): AGENTS.md already serves this purpose
- DX-04 (no lockfile): false positive — `bun.lock` exists
- TD-08 (module-level mutable cache): MED confidence, complex refactor for small project — not worth the risk
- TD-10 (BaseEngine mixes concerns): true but multi-day refactor — defer until codebase grows
- DIR-03 (implement engine subclasses): L effort, requires integration testing against real servers — defer
